# Self-hosted CI (Woodpecker). No GitHub Actions by design.
when:
  - event: [push, pull_request]

steps:
  - name: check
    image: python:3.12-slim
    commands:
      - pip install -e ".[dev]"
      - ruff check .
      - ruff format --check .
      - pytest -q
