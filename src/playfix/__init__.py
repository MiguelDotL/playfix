"""PlayFix — make social media links play in Discord.

A small, self-hostable Discord bot that rewrites social-media links to their
embed-fixer domain (so videos actually play) and — optionally — reposts them via
webhook as the original author, so the fixed embed looks like it came from them.
"""

__version__ = "0.1.0"
