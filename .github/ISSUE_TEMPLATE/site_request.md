---
name: Add / fix a platform
about: Request support for a site, or report a wrong fixer domain
labels: platform
---

**Platform**
e.g. Threads

**Source domain(s)**
e.g. `threads.net`, `threads.com`

**Fixer domain you'd use**
e.g. `fixthreads.net` — and why you trust it (well-known, maintained, not a throwaway host)

**Example content URLs**
- one that SHOULD be fixed:
- one that should NOT be (a profile / the site root):
